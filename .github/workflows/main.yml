name: CI

on: [push]
jobs:

  build:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v1

      - uses: ./.github/actions/docker
        with:
          args: source activate lcdb-wf-test; STAR

