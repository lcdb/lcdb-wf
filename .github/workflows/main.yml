name: CI

on: [push]
jobs:

  build:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v1

      - uses: ./dockeraction.yml

      - name: STAR available?
        shell: bash
        run: |
          source activate lcdb-wf-test
          STAR --version

