name: CI

on: [push]
jobs:

  build:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v1

      - uses: ./
      - uses: dockeraction.yml@v1

      - name: STAR available?
        shell: bash
        run: |
          source activate lcdb-wf-test
          STAR --version

